import React, { PureComponent } from 'react'
import { Page } from 'components'

import styles from './index.less'

class Dataset extends PureComponent {
    render() {
        return (
            <Page className={styles.dataset}>
            </Page>
            )
    }
}

export default Dataset
